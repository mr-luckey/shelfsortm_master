/// Canonical mechanic id strings used in level JSON.
abstract final class MechanicIds {
  static const hiddenBackRow = 'hidden_back_row';
  static const movingBottomTray = 'moving_bottom_tray';
  static const slidingShelves = 'sliding_shelves';
  static const rotatingTray = 'rotating_tray';
  static const conveyorShelf = 'conveyor_shelf';
  static const conveyorTray = 'conveyor_tray';
  static const lockedItems = 'locked_items';
  static const mysteryBoxes = 'mystery_boxes';
  static const stackedItems = 'stacked_items';
  static const frozenItems = 'frozen_items';
  static const movingDivider = 'moving_divider';
  static const chainRelease = 'chain_release';

  static const all = [
    hiddenBackRow,
    movingBottomTray,
    slidingShelves,
    rotatingTray,
    conveyorShelf,
    conveyorTray,
    lockedItems,
    mysteryBoxes,
    stackedItems,
    frozenItems,
    movingDivider,
    chainRelease,
  ];
}
